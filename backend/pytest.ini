[pytest]
asyncio_mode = auto
# Only collect the real suite under tests/. The repo root contains manual
# scratch scripts (integration_test.py, test_tl.py) that run container-only
# paths at import time and are not collectable pytest tests.
testpaths = tests
